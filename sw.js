const CACHE='elite-shoes-v1.4.3';
const ASSETS=['./manifest.json','./version.json','./sw.js'];
const FIX=`<script>(function(){try{const k='elite_shoes',x=JSON.parse(localStorage.getItem(k)||'null');if(x&&Array.isArray(x.products)){let changed=false;x.products.forEach(p=>{if(p.qty===undefined||p.qty===null||p.qty===''){p.qty=Number(p.quantity??p.stock??p.pairs??p.availableQty??0)||0;changed=true}else if(typeof p.qty==='string'){p.qty=Number(p.qty)||0;changed=true}});if(changed)localStorage.setItem(k,JSON.stringify(x))}}catch(e){}})();</script>`;
async function prepareApp(){const r=await fetch('./app.html',{cache:'no-store'});let t=await r.text();if(!t.includes('id="refreshBtn"'))t=t.replace('<button class="btn" id="updateBtn">↻ Check Update</button>','<div><button class="btn" id="refreshBtn" onclick="location.reload()">⟳ Refresh</button> <button class="btn" id="updateBtn">↻ Check Update</button></div>');if(!t.includes("const k='elite_shoes'"))t=t.replace('</body>',FIX+'</body>');return new Response(t,{headers:{'Content-Type':'text/html; charset=utf-8'}})}
self.addEventListener('install',e=>e.waitUntil((async()=>{const c=await caches.open(CACHE);await Promise.all(ASSETS.map(x=>fetch(x,{cache:'no-store'}).then(r=>c.put(x,r))));await c.put('./app.html',await prepareApp());await self.skipWaiting()})()));
self.addEventListener('activate',e=>e.waitUntil(caches.keys().then(keys=>Promise.all(keys.filter(k=>k!==CACHE).map(k=>caches.delete(k)))).then(()=>self.clients.claim())));
self.addEventListener('fetch',e=>{if(e.request.method!=='GET')return;const u=new URL(e.request.url);if(u.pathname.endsWith('/app.html')){e.respondWith(caches.match('./app.html').then(c=>c||prepareApp()));return}e.respondWith(caches.match(e.request).then(c=>c||fetch(e.request).then(r=>{const cp=r.clone();caches.open(CACHE).then(x=>x.put(e.request,cp));return r}))) });